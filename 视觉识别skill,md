视觉识别 skill —— 现场人员识别 + 动作质量评估 + 实时语音互动
目标：demo 进行中，摄像头看向现场一起做操的人（A/B/C 三位）， 识别出"谁"和"做得标准不标准"，通过已有语音接口及时给出日语反馈。 语音侧接口已预留（demo_voice.py 的 VoiceCoach.coach_feedback）， 本文档规划视觉侧如何产出 observations 并对接。

观察结果格式（已定，勿改）：
coach.coach_feedback([{"name": "A", "advice": "もう少し腕を上げましょう"}])
# 列表为空 = 没识别到人 = 不出声（已实现）
0. 一页结论（没时间细看就照这个做）
决策点	推荐方案	理由
认人	彩色底板 + 大字母（人看字母，代码识颜色）	布料上 OCR 识字很脆弱，HSV 识颜色 20 行代码就很稳
认人兜底	站位法：左=A 中=B 右=C，按画面 x 坐标分配	零标记、零失败模式，彩排时先用它跑通
姿态估计	MediaPipe Pose（或 YOLOv8n-pose）	CPU 实时、pip 装完即用、关键点够用
动作评估	不做动作识别，只做规则打分：机器人自己知道现在该做第几节，视觉只量"幅度/角度达标没"	把难题（识别在做什么）换成简单题（量做得够不够大）
反馈时机	每节前 8 拍观察、第 9~16 拍之间播 1 句；每节最多 1 句，轮流点名	不跟コツ报幕抢通道，不轰炸观众
运行位置	笔记本 + USB/内置摄像头面向人群，视觉进程独立跑，结果喂给语音进程	不碰 G1 头部相机取流，链路风险最低
保底	四级降级方案（见 §6），Level 0 是写死脚本，任何时候可退回	黑客松第一原则：demo 不能翻车
1. 时间轴与视觉的关系（写死的部分，这是我们最大的优势）
demo 的节奏完全由 demo_viewer.section_timeline() 决定，视觉不需要判断 "现在大家在做什么动作"——机器人播放时钟本身就是标准答案：

0 ~ 14s      挨拶（视觉此时做：找人、锁定 A/B/C 身份、建立跟踪）
14 ~ 26.5s   ① 伸びの運動（8拍，约6.2s×2 …实际①只有8拍）
之后每节      16拍 ≈ 12.5s（BPM=77），节间缓冲 1.0s（DEMO_GAP 可调大）
总长          ≈ 80s
视觉主循环拿到 (motion_key, sim_t)（和 VoiceCoach.on_tick 同一协议）， 就知道"此刻这些人应该在做第③节的第 5 拍"，于是只需回答一个简单问题： 关键点量出来的幅度/角度，达标了吗？

集成方式：视觉进程独立跑，每节产出一条评估结果写入队列/文件/socket； 语音进程在合适的提示点读取并调用 coach_feedback。两进程解耦， 视觉挂了语音照常跑（自动退化为"无互动"，符合"没识别到人就不出声"）。

2. 认人：如何区分 A / B / C
2.1 推荐主方案：彩色底板 + 大字母（人机两用）
打印三张 A4：红底白字 A、蓝底白字 B、黄底白字 C，贴在胸口。

观众和主持人看到的是字母（符合"衣服上贴文字"的原始设想）；
代码识别的是颜色（HSV 阈值 + 连通域面积过滤），完全绕开 OCR。
实现：cv2.inRange 三个颜色区间 → 取每个颜色最大连通域中心 → 与人体检测框做包含匹配 → 该人 = 对应字母。
颜色选择注意：避开现场背景色和大家衣服的颜色；红色在 HSV 里跨 0° 分界， 要写两段区间；黄色容易和肤色/木地板混，阈值现场标定（见 §7 彩排清单）。
2.2 兜底方案：站位法（强烈建议彩排先用这个跑通全链路）
三人固定站位：面向摄像头左=A、中=B、右=C。检测到的人按画面 x 坐标 排序即得身份。零道具、零标定、不会错——唯一约束是"别换位置"， 现场口头约定即可。Level 1 降级方案就是它（§6）。

2.3 备选：ArUco 码（技术上最稳，观感其次）
OpenCV 自带 cv2.aruco，打印 ≥15cm 的码贴胸口，直接返回 ID， 运动模糊下也稳。缺点是观众看到的是二维码不是字母——可以码旁边印字母。 如果 2.1 的颜色现场标定不顺利，1 小时内可切换到这个。

2.4 不推荐：直接 OCR 识别字母
布料起皱、身体转动、动作模糊、光照不均，OCR（Tesseract/PaddleOCR） 在动态衣物上的召回率不可控，且每帧跑 OCR 也慢。不要在黑客松赌这个。

2.5 身份保持（跟踪）
标记只在初始锁定时用（挨拶那 14s 是黄金窗口：大家正面站好还没动）。 锁定后用 IoU 贴框跟踪（上一帧的框和这一帧重叠度最大者=同一人） 维持身份，标记被手臂挡住也不丢。三个人、站着不走动的场景， IoU 跟踪足够，不需要 ByteTrack/DeepSORT。每节开头（缓冲那 1s， 大家静止）可以用颜色重新校验一次身份，防漂移。

3. 动作质量评估：每节量什么
姿态估计统一输出 2D 关键点（鼻、肩、肘、腕、髋、膝、踝）。 原则：只判"幅度类"指标，不判"呼吸/发力/脚跟"这类相机看不见的东西。 每节取整节（或每 8 拍周期）的极值来打分，绝不按单帧判—— 单帧正好处在动作中途，人人都"不达标"。

节	该节要点（コツ）	可量化指标（2D关键点）	达标阈值（起点值，现场标定）	不达标时的台词
① 伸び	手臂伸直、举高	a) 腕关键点 y 高于鼻 y；b) 肘角(肩-肘-腕) > 160°	每个8拍周期内 a、b 同时出现过	もう少し腕を高く上げましょう
② 腕振り屈伸	摆臂有节奏、屈膝	a) 腕 y 的振荡幅度 > 0.3×躯干长；b) 膝角(髋-膝-踝)最小值 < 150°	16拍内 a 或 b 达标（脚跟上下看不见，不判）	腕を大きく振りましょう
③ 腕回し	放松、画大圈	腕 y 的最高点高于头顶、最低点低于髋 → 圈够大	每8拍内两个极值都出现	腕をもっと大きく回しましょう
④ 胸反らし	展臂扩胸、头别过仰	两腕 x 间距最大值 > 1.8×肩宽（侧平举展开）	16拍内出现过	胸を大きく開きましょう
⑤ 横曲げ	侧弯、不前倾	肩中点-髋中点连线与竖直方向夹角 > 20°，左右各一次	每8拍一侧	体をしっかり横に曲げましょう
所有长度阈值用该人自己的肩宽/躯干长归一化，不用像素绝对值， 这样离相机远近不影响判定。
表扬台词（达标时）：いい感じですね！頑張りましょう / とても上手です！その調子！——台词集是有限集合 （姓名3 × 每节台词2），demo 开始前全部 TTS 预取（走 tts_qwen.prefetch）， 到点零等待。
关键点置信度低于阈值（被挡/出画）→ 该人该节弃权不评，宁可不说， 不说错。
姿态估计选型
方案	优点	注意
MediaPipe Pose（推荐）	pip install mediapipe 即用，CPU 30fps，33 个关键点带置信度	单人模型——多人场景需先用人体检测切三个 ROI 再分别跑，或直接换下面这个
YOLOv8n-pose（多人首选）	一次前向出所有人的框+17关键点，天然多人	pip install ultralytics，CPU 上 n 版约 10~20fps，够用（我们按节评估，不需要每帧）
三个人同框 → 直接用 YOLOv8n-pose 更省事，框还能顺便给 §2 做标记匹配。

4. 反馈编排：什么时候说、说什么、说多少
语音通道上已有的占用：节间缓冲+节首 = コツ报幕。互动反馈的空位：

每节第 9~16 拍（コツ长句已说完，报数默认关）→ 主反馈窗。 前 8 拍采集打分，第 9 拍起找 TTS 空闲时机播 1 句。
规则：
每节最多 1 句，三人轮流做主角（①A ②B ③C ④A ⑤B…）， 避免总说一个人、也避免语音轰炸。
纠正要有把握，表扬可以宽松：指标明确不达标才纠正； 达标或拿不准 → 表扬。说错表扬没事，说错批评很尴尬。
整场保证每人至少被表扬一次（第⑤节结算：还没被点过名的人补一句好话）。
_tts_busy() 为真就顺延到下一拍，第 14 拍后还没播出去就放弃本节 （沿用报数"忙则跳过不排队"的原则，不漂移不堆积）。
结尾感谢（みんな、ありがとう！）之后，若三人整场总体达标， 可加一句总评（可选，写死即可）。
5. 系统架构与接口
[USB/笔记本摄像头] ──帧──> vision_coach.py（独立进程）
    ├─ 每帧：YOLOv8n-pose → 人框+关键点
    ├─ 挨拶窗口：颜色标记/站位 → 锁定 A/B/C，之后 IoU 跟踪
    ├─ 按 (motion_key, sim_t) 累积各人指标极值
    └─ 每节第8拍末：产出该节评估 → 写入 observations 通道
                                        │
demo_voice.py（现有进程） <──读取────────┘
    └─ 主反馈窗内调用 coach.coach_feedback(observations)
observations 通道：最简单用一个 JSON 文件原子写+轮询 （observations.json，内容即 [{"name","advice"}]）， 10 分钟能写完、跨进程无依赖；讲究一点用 localhost UDP/socket。
时钟同步：仿真态直接把 on_tick 同一份 (key, t) 广播给视觉进程； 真机态视觉进程收执行循环同一协议（文档头注释已注明真机接法一致）。 离线兜底：视觉进程收到"demo 开始"信号后自己按写死的时间轴推算当前节。
新增文件建议：vision_coach.py（主程序）、vision_rules.py （§3 的指标与阈值表，独立成文件方便现场改数字——和 motions.py 改角度重跑的调参循环一个思路）。
6. 降级路线（按此顺序开发，做到哪级 demo 用哪级）
级别	内容	开发量	demo 效果
Level 0	全写死：按时间表播预先写好的三句互动（无摄像头）	0.5h	观众看不出区别，绝对安全的兜底
Level 1	只认人不评动作：检测到人→按站位法定 A/B/C→随机/轮换表扬（真名字、真检测）	2h	"没人就不出声"是真的，点名是真的
Level 2	评 ①⑤ 两节（指标最简单直观：手举没举过头、身体弯没弯）+ 其余节表扬	+3h	纠正建议开始"言之有物"
Level 3	五节全评 + 彩色标记认人 + 每人至少一赞	+3h	完整体
先把 Level 0 排进彩排（它同时验证了反馈窗时机和 TTS 台词）， 再逐级替换数据来源。每一级都保持 observations 接口不变，随时可回退。

7. 给现场一起做操的人的建议（什么动作好识别）
站位与环境：

三人一排正面朝摄像头，间距 ≥ 一臂（挥臂不互相遮挡），距相机 2.5~4m， 保证全身入画（脚踝要在画面里，⑤的躯干角依赖髋部）。
别背对窗户/强光源（逆光是关键点检测头号杀手）；地面/背景尽量和衣服颜色 有反差；衣袖别太宽大（袖子把肘腕关键点带偏）。
标记贴胸口正中，别贴肚子（容易被摆臂挡住）。
动作习惯（很重要，彩排时明确告诉大家）：

幅度做大、做夸张——我们评的是极值，幅度大 = 好识别 = 多被表扬。
在"额状面"（与相机平行的平面）里运动最好识别： 手臂从体侧向上举 ✅、侧平举展开 ✅、身体左右侧弯 ✅、原地屈膝 ✅。
朝相机方向的深度运动很难识别，尽量避免：手臂朝前伸向相机（透视缩短， 看起来没动）❌、鞠躬式前倾 ❌。第③节甩臂请在身体两侧画正面看是圆的大圈。
踮脚跟、深呼吸、面部朝向——相机基本看不出来，不会被评， 这些细节做给现场观众看即可。
跟着机器人的节拍走：我们按拍号窗口取极值，节奏对了才能在正确的窗口里 量到你的最大幅度。
如果某人想被"纠正"一次制造节目效果：故意手举一半（①）或圈画小（③）， 这两个指标最灵敏，几乎必然触发纠正台词——可以排进演出脚本。

8. 开发排期建议
[1h] 链路先通：vision_coach.py 骨架 + observations JSON 通道 + demo_voice.py 读取侧，用 Level 0 假数据全链路出声。
[1h] 摄像头+检测：YOLOv8n-pose 出人框和关键点，画面叠加可视化 （调试窗口保留，现场标定要用）。
[1h] 认人：站位法 → 彩色标记（备 ArUco 方案）。
[2h] 规则打分：先 ①⑤，再 ③④②；阈值全部放 vision_rules.py 顶部。
[1h] 反馈编排：轮流点名、每节一句、忙则跳过、每人至少一赞。
[1h] 彩排标定：现场光照下调颜色阈值和动作阈值；跑三遍完整 demo， 确认互动语音不和コツ抢通道。
依赖安装：pip install ultralytics opencv-python（ArUco 在 opencv 里自带； MediaPipe 备选 pip install mediapipe）。

9. 已知风险与对策
风险	对策
现场光照和彩排差异大，颜色阈值失效	站位法兜底（§2.2）；调试窗口现场重标 HSV
观众/路人入画被当成 A/B/C	只认"挨拶窗口内锁定的三个框"，后来入画的人不分配身份
关键点抖动导致误判	指标取整节极值 + 置信度门限 + 弃权机制（§3）
视觉进程崩溃	语音侧读不到 observations = 空列表 = 静默，demo 不受影响
反馈台词播出时已换节，显得迟钝	第 14 拍后放弃本节（§4 规则 4）
TTS 网络抖动	台词有限集合开场前全部预取（已有 prefetch 机制）
